<%= form_for(@t_film) do |f| %>
  <% if @t_film.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@t_film.errors.count, "error") %> prohibited this t_film from being saved:</h2>

      <ul>
      <% @t_film.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :size %><br>
    <%= f.number_field :size %>
  </div>
  <div class="field">
    <%= f.label :torrent %><br>
    <%= f.text_field :torrent %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
