<%= simple_form_for(@torrent) do |f| %>
    <% if @torrent.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@torrent.errors.count, "error") %> prohibited this film from being saved:</h2>

          <ul>
            <% @torrent.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <div class="row">
      <div class="small-8 columns">
        <%= f.input :name, label: 'Nom' %>
        <%= f.input :comment, label: 'Commentaires' %>
      </div>
    </div>
    <div class="row">
      <div class="small-8 columns">
        <%= f.button :submit, 'Valider', class: 'small radius' %>
      </div>
    </div>
<% end %>