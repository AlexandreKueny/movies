<%= simple_form_for(@film) do |f| %>
    <% if @film.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@film.errors.count, "error") %> prohibited this film from being saved:</h2>

          <ul>
            <% @film.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <div class="row">
      <div class="small-8 columns">
        <%= f.input :name, label: 'Nom' %>
        <%= f.input :comment, label: 'Commentaires' %>
        <%= f.association :torrent, prompt: 'Choisir un torrent', include_blank: true %>
      </div>
    </div>
    <div class="row">
      <div class="small-8 columns">
        <%= f.button :submit, 'Valider', class: 'small radius' %>
      </div>
    </div>
<% end %>
